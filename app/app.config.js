

export default defineAppConfig({
  ui: {

    badge: {
      defaultVariants: {
        color: 'neutral',
      },
    },

    button: {
      defaultVariants: {
        color: 'neutral',
      },
    },

    calendar: {
      defaultVariants: {
        color: 'neutral',
      },
    },

    card: {
      slots: {
        body: 'p-3 sm:p-3',
      },
    },

    input: {
      defaultVariants: {
        color: 'neutral',
      },
    },

    select: {
      defaultVariants: {
        color: 'neutral',
      },
    },

    tabs: {
      defaultVariants: {
        color: 'neutral',
      },
    },

  },
});
